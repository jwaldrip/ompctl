{
  "name": "@ompd/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "bun test test/",
    "verify:icons": "bun run ../../scripts/verify-icons.ts"
  },
  "dependencies": {
    "@ompd/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.3.1"
  }
}
